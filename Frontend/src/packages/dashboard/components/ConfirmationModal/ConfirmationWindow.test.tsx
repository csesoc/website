import { render } from 'src/cse-testing-lib';
import ConfirmationWindow from './ConfirmationWindow';

describe("Confirmation window tests", () => {
  it("Confirmation window rendered when New Page button is clicked", () => {
    // TODO
  });
})
